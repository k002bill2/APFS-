# 메뉴 대조표 — 메뉴구성도(xlsx) ↔ `APFS_DATA.MENU`(앱)

> 좌: `APFS-2026-120-PP01_…_메뉴매칭_v0.2.xlsx` **메뉴구성도** 시트의 「새로운 메뉴 구성」(to-be)
> 우: `src/dash/data.ts` 의 `MENU` (2026-09-15 기준 HEAD `7bd5091`)
> 대조 키: `업무영역 > 중분류 > 리프 라벨` (NFC 정규화, 공백 trim)

## 결론

- **위치·라벨 완전 일치 133개.** 앱은 이미 xlsx 「신규 메뉴 구성」을 구현하고 있다 — 현행(as-is) 트리가 아니다.
- 실제 불일치는 **라벨 4건**뿐. 나머지 차이는 전부 **의도적 범위 차이**(로그인·관리자(기존) 미반영, 대시보드 추가)다.
- 앱 `관리자`(8리프/3중분류)는 xlsx **관리자 (신규)** 블록과 정확히 일치 — 관리자 (기존) 12리프는 채택되지 않았다. (단 이 대응은 시트가 명시한 게 아니라 모양·화면파일 근거로 이 문서가 내린 판단이다.)
- **순서까지 일치.** 중분류 등장 순서(32개)와 중분류별 리프 순서가 양쪽 모두 동일 — 집합뿐 아니라 배열로 비교해 확인했다. LNB 렌더 순서·`ALLMENU` 즐겨찾기 키가 `MENU` 배열 순서를 그대로 쓰므로 이 검증이 필요하다.

### 검증 근거 (실행 결과)

```
xlsx 신규 리프: 원본셀 152  파싱 152   (as-is 128/128, 고아 0건)
app  리프     : 137 (+대시보드 1) / 중분류 32 / 대분류 7
위치+라벨 완전일치 133 · xlsx만 19 · 앱만 5
  → 19 = 라벨불일치 4 + 블록 미반영 15(관리자(기존) 12 + 로그인 3)
  → 5  = 라벨불일치 4 + 대시보드 1
중분류 등장 순서 일치: True · 리프 순서 불일치 중분류: 0 / 32
```

## 1. 업무영역별 수량 대조

| 업무영역 | xlsx 중분류 | 앱 중분류 | xlsx 리프 | 앱 리프 | 상태 |
|---|---:|---:|---:|---:|---|
| 투자자산관리 | 6 | 6 | 32 | 32 | ✅ 수량 일치 |
| 조기경보 | 4 | 4 | 26 | 26 | ✅ 수량 일치 |
| 자펀드 보고 | 7 | 7 | 32 | 32 | ✅ 수량 일치 |
| 회계 | 8 | 8 | 26 | 26 | ✅ 수량 일치 |
| 부처보고 | 2 | 2 | 2 | 2 | ✅ 수량 일치 |
| 수탁보고 | 2 | 2 | 11 | 11 | ✅ 수량 일치 |
| 관리자 (기존) | 5 | — | 12 | — | ⛔ 앱 미반영 |
| 로그인 | 1 | — | 3 | — | ⛔ 앱 미반영 |
| 관리자 (신규) | 3 | 3 | 8 | 8 | ✅ 수량 일치 |
| 대시보드 | — | — | — | 1 | ➕ 앱에만 |
| **합계** | **38** | **32** | **152** | **137** | |

## 2. 라벨 불일치 (같은 위치, 이름만 다름)

| 업무영역 > 중분류 | xlsx 라벨 | 앱 라벨 | 판정 | 비고 |
|---|---|---|---|---|
| 투자자산관리 > 조합관리 | `자편드별조합원조회` | `자펀드별조합원조회` | **앱이 맞음** | xlsx 원본 오타(자**편**드). 앱은 `path:"fund-member"` 라 route 영향 없음 |
| 자펀드 보고 > 운영기관정보 | `공동GP펀드별 인력현황` | `공통GP펀드별 인력현황` | **xlsx가 맞음** | 앱 오타(공**통**→공**동**). path 없음 = 라벨이 route 키 |
| 자펀드 보고 > 운영기관정보 | `운용사 재무정보` | `운용사 재무보고` | **xlsx가 맞음** | 앱이 「보고」로 잘못 적음. path 없음 = 라벨이 route 키 |
| 조기경보 > 가치평가 | `예외사항레포트` | `예외사항리포트` | **판단 필요** | 스펙 기준이면 xlsx(`레포트`), 어문규범 기준이면 앱(`리포트`)이 맞다 — 선택 사항. path 없음 = 라벨이 route 키 |

> ⚠️ 라벨 수정 시 주의: 리프에 `path:` 가 없으면 **라벨이 곧 route 키**다(`leaf.path || leaf.label` 규약, `data.ts:104`). 다만 위 2~4행(`path` 없는 3건)은 `src/` 전체에서 `data.ts` 외 등장이 없어 `schemas/` 에 등록된 route 가 아니다 — `_default.ts` 폴백 화면이라 라벨을 바꿔도 깨지는 배선은 없다.

## 3. xlsx에만 있는 메뉴 (앱 미반영)

총 15개. 개별 누락이 아니라 **블록 단위 미반영**이다.

| 업무영역 | 중분류 | 메뉴 | 원본 매칭/화면 |
|---|---|---|---|
| 관리자 (기존) | 게시판 | 게시판 관리 | 관리자 > 게시판 > 게시판 관리 |
| 관리자 (기존) | 보안 | 개인정보 접속관리 | 관리자 > 보안 > 개인정보 접속관리 |
| 관리자 (기존) | 보안 | 사용자별 권한조회 | 관리자 > 보안 > 사용자별 권한조회 / brief > 관리자 > 사용자권한관리 |
| 관리자 (기존) | 보안 | 사용자별 로그조회 | 관리자 > 보안 > 사용자별 로그조회 |
| 관리자 (기존) | 사용자 관리 | 사용자 관리 | 관리자 > 사용자 관리 > 사용자 관리 / ffms > 관리자 > 사용자관리 / brief > 관리자 > 사용자관리 |
| 관리자 (기존) | 사용자 관리 | 사용자 권한 관리 | 관리자 > 사용자 관리 > 사용자 권한 관리 / risk > 관리자 > 사용자권한 관리 / brief > 관리자 > 사용자권한관리 / 사용자조합권한관리 |
| 관리자 (기존) | 시스템 관리 | 공통코드 관리 | 관리자 > 시스템 관리 > 공통코드 관리 / ffms > 관리자 > 공통코드관리 / risk > 관리자 > 공통코드 관리 |
| 관리자 (기존) | 시스템 관리 | 도움말 관리 | 관리자 > 시스템 관리 > 도움말 관리 / risk > 관리자 > 도움말 관리 |
| 관리자 (기존) | 시스템 관리 | 메뉴 관리 | 관리자 > 시스템 관리 > 메뉴 관리 / ffms > 관리자 > 메뉴관리 / risk > 관리자 > 메뉴 관리 |
| 관리자 (기존) | 외부연동 | 연계 모니터링(API) | 관리자 > 외부연동 > 연계 모니터링(API) |
| 관리자 (기존) | 외부연동 | 자펀드 보고 양식관리(운용사ERP) | 관리자 > 외부연동 > 자펀드 보고 양식관리(운용사ERP) / ffms > 사후보고관리 > 보고양식관리 |
| 관리자 (기존) | 외부연동 | 자펀드 코드관리(운용사ERP&수탁기관) | 관리자 > 외부연동 > 자펀드 코드관리(운용사ERP&수탁기관) |
| 로그인 | 로그인 | 로그인 | S0_001_로그인.html |
| 로그인 | 로그인 | 발급온보딩 | S0_002_발급온보딩.html |
| 로그인 | 로그인 | 초대온보딩 | S0_003_초대온보딩_운용사.html |

## 4. 앱에만 있는 메뉴

| 업무영역 | 중분류 | 메뉴 | 성격 |
|---|---|---|---|
| 대시보드 | — | 대시보드 | 프로토타입 홈 화면 (xlsx 메뉴 체계 밖) |

## 5. 문서 드리프트 — `CLAUDE.md` 기재 수치

| 항목 | CLAUDE.md 기재 | 실측(`data.ts` HEAD) | 차이 |
|---|---:|---:|---:|
| 대분류 | 7 (+대시보드) | 7 (+대시보드) | 0 |
| 중분류 | 34 | 32 | -2 |
| 리프 | 141 | 137 | -4 |

또한 `CLAUDE.md`·`data.ts:102` 는 이 트리를 "현행시스템 메뉴 구조표 1:1 정본"이라 적었으나, 위 1절이 보여주듯 실제로는 xlsx **신규(to-be) 구성**과 일치한다 — 출처 기술도 함께 손봐야 한다.
