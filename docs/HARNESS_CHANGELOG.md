# 하네스 변경 이력

하네스(에이전트·스킬·훅·설정)의 진화를 추적한다. 구성요소 추가/삭제/수정 시 여기에 기록한다.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-20 | 훅 fallback 경로 수정(`WORK/WORKSPACE/APFS`→`Work/APFS`), 모델 핀 `claude-opus-4-8[1m]`로 변경, PreCompact 훅 문서화, 본 변경이력 신설 | `settings.json`, `CLAUDE.md` | 하네스 현황 감사에서 발견된 drift 3건 정리 |
| 2026-06-23 | `main`/`master`에서 `git commit`·`push` 직행을 차단하는 PreToolUse(Bash) 가드 훅 추가(`block-main-write.sh`) | `settings.json`, `.claude/hooks/block-main-write.sh` | main push의 Vercel 프로덕션 자동배포 직행 사고 방지 |
| 2026-06-23 | `commit-push-pr`를 skill→command로 정리(인자 플래그·`allowed-tools` 확보, 안전 규약 유지), 중복 정의 제거 | `.claude/commands/commit-push-pr.md` | `/commit-push-pr` 트리거 단일화 + 사용자 전용 git 워크플로엔 command가 적합 |
| 2026-06-25 | `responsive-ui` 스킬의 입력 폰트 규칙을 `≥16px`→`=14px(프로젝트 표준)`으로 정정(iOS 자동확대 지식·탈출구는 보존). 체크리스트5·쿡북F·검증·함정표 4곳 | `.claude/skills/responsive-ui/SKILL.md` | 코드 실제 컨벤션(폼 컨트롤 14px 통일, 커밋 a677118)과 스킬 간 drift 해소 — 스킬을 따르면 오히려 컨벤션 위반이던 역설 제거 |
| 2026-06-29 | `color-tokens` 스킬 신설 + 트리거 등록 + 토큰 시스템 문서(`docs/COLOR_TOKENS.md`) 작성 | `.claude/skills/color-tokens/SKILL.md`, `.claude/hooks/skill-rules.json`, `docs/COLOR_TOKENS.md` | 팔레트 인디고 전환·하드코딩 색 전수 토큰화(PR #29)에서 정립한 규약(적응형 vs 고정 전경 분기=adaptive-bg 함정) 영속화 |
| 2026-06-30 | `apfs-aggrid`(AG Grid 본체)·`apfs-form-modal`(스키마 주도 CRUD 모달) 스킬 2종 신설 + 트리거 등록 | `.claude/skills/apfs-aggrid/SKILL.md`, `.claude/skills/apfs-form-modal/SKILL.md`, `.claude/hooks/skill-rules.json` | 골드 레퍼런스(그리드=`asset_funding.tsx` "모태펀드 조성·출자 현황표", 모달=`투자기업정보(통합)`)의 비자명 함정(모듈 등록·회색선택 격리·pinned 상수·FIELD_CONTROLS SSOT·2단 적응) 영속화. apfs-grid(프레임)와 본체/모달 경계 분리 |
| 2026-06-30 | `z-index` 스킬 신설 + 트리거 등록 + 쌓임맥락 규약 문서(`docs/Z_INDEX.md`) 작성 | `.claude/skills/z-index/SKILL.md`, `.claude/hooks/skill-rules.json`, `docs/Z_INDEX.md` | 접힘 LNB 측면 플라이아웃이 메인 카드에 가려진 버그에서 정립한 sticky/transform 조상 쌓임맥락 트랩 규약(비포털 오버레이는 조상 맥락에 갇힘 → 소비처 nav에 양수 z, 토큰 스케일만 사용) 영속화 |
| 2026-06-30 | `apfs-datepicker` 스킬 신설 + 트리거 등록(`shadcn-radix-calendar-datepicker` 메모리 승격·원본은 포인터로 슬림화) · `apfs-form-modal`에 무거운컨트롤 포인터+노후 `date`행 정정 · `heavy-form-controls` 메모리에 required-richtext false-pass 잠복버그 추가 | `.claude/skills/apfs-datepicker/SKILL.md`, `.claude/skills/apfs-form-modal/SKILL.md`, `.claude/hooks/skill-rules.json`, 메모리 2건 | "컴포넌트별 스킬 신설" 검토 결과 — 베이스라인 테스트(3에이전트 3/3)로 무거운-필드는 갭 미확인→스킬 신설 대신 포인터만. 달력은 전 화면 공용 프리미티브라 결정성 명분으로 메모리→스킬 승격. 테스트가 부수로 발견한 잠복버그 영속화 |
| 2026-07-03 | `apfs-aggrid` 스킬에 "우클릭 컨텍스트 메뉴 / pivot(Community 경계)" 규약 추가(내장 메뉴·pivot=Enterprise→pivot 보류·메뉴는 `onCellContextMenu`+`preventDefaultOnContextMenu`+공유 `row_context_menu.tsx`, rowPinned 가드·항목 14px·복사 mn()마스킹) + `aggrid-community-context-menu` 메모리 신설 | `.claude/skills/apfs-aggrid/SKILL.md`, 메모리 1건 | context menu 기능 구현(PR #54)에서 정립한 Community 대체 패턴 영속화 — Set Filter/Excel과 동일한 Enterprise 게이트 우회 계보에 편입 |
| 2026-07-03 | `web-a11y`(웹 접근성/KWCAG·WCAG) 스킬 신설 + 트리거 등록(페이지 생성/수정마다 광범위 발동) + 원칙 문서 `docs/A11Y.md` 정리 | `.claude/skills/web-a11y/SKILL.md`, `.claude/hooks/skill-rules.json`, `docs/A11Y.md` | 접근성 규약 영속화 — 코드베이스 실측으로 정본 패턴(IconBtn label→aria·Field `<label>` 암묵연결·Radix Dialog 트랩·alert/skeleton/spinner 라이브리전·전역 focus-visible/reduced-motion)과 실제 갭(placeholder≠접근名, 복합컨트롤 `<label>` 오연결)을 코드 위치와 함께 규약화. 대비/터치타깃/색은 color-tokens·responsive-ui로 크로스링크(중복 금지) |
| 2026-07-11 | `dashboard-ui` 스킬에 "차트 정책" 섹션 추가 — 자체 SVG(charts.tsx) 확장이 유일 경로, 외부 차트 라이브러리 도입=사용자 재승인 사항(에이전트 권한 밖), Highcharts·amCharts·AG Charts Enterprise·FusionCharts는 라이선스 문제로 후보 영구 제외, 재검토 시 MIT/Apache/ISC만 | `.claude/skills/dashboard-ui/SKILL.md`, 메모리 1건 | 사용자 확정 정책 영속화. TDD 검증: 베이스라인(규칙 無)에서 에이전트가 "일정상 자체구현 비현실적"으로 ECharts 설치 결정(RED) → 정책 추가 후 동일 시나리오에서 charts.tsx 확장 주경로+라이브러리는 재승인 표기로 전환(GREEN). 부수 발견: dashboard-ui 스킬 본문 pre-Vite stale(같은 날 전면 갱신, 아래 행) |
| 2026-07-11 | `dashboard-ui` 스킬 전면 갱신 — pre-Vite stale 제거(Babel standalone·window IIFE·"빌드 없음"·apfs-bundle 편집 절차·레거시 HTML 검증 → Vite/ES modules/GenericListPage 폴백/mn·MT 마스크/npm run dev 검증), 관련 스킬 크로스링크 섹션 신설, RBAC 카운트 실측 정정(admin 8/manager 7/viewer 4) | `.claude/skills/dashboard-ui/SKILL.md`, `CLAUDE.md` | 2026-06 Vite 마이그레이션 이후 방치된 drift 해소. Codex 리뷰 반영: 차트 툴팁 마스킹 과장 정정(ComposedBars·LineTrend·Treemap 툴팁 마스킹 미적용 확인), RBAC 9/8/5→8/7/4, 변경 이력 모순 해소 |
| 2026-07-12 | `block-main-write.sh`에 삭제 push 예외 추가 — main/master에서도 "정적으로 안전한 단일 `--delete`/`-d` push"만 통과, 삭제 대상이 main/master(부분 ref명 `heads/main` 포함)면 계속 차단. 위협 모델 명시(실수 방지 best-effort, 의도적 우회 경계 아님) | `.claude/hooks/block-main-write.sh` | 병합된 피처 브랜치 정리 시 `git push origin --delete <branch>`가 배포와 무관한데도 오탐 차단되던 마찰 해소. `--delete`는 git이 모든 ref를 삭제 처리(콘텐츠 push 혼용 불가)라 배포 무관. Codex 적대적 리뷰 3라운드 P1 6건 + advisor 반영해 fail-closed로 강화: 셸 메타문자(`;`/`&`/`\|`/`$`/`` ` ``/`(){}<>*?`)·백슬래시·개행·`--no-delete`·콜론 refspec 단독형(`HEAD :old`)·따옴표 `"main"`은 예외 제외, ref 검사는 `((refs/)?heads/)?`로 넓혀 `heads/main` fail-open 차단(단 `feat/main`은 통과 유지). 검증: git shim main 시뮬 매트릭스 전부 green. ※PR #95가 초기 v1(우회 잔존)으로 조기 머지돼, 강화본은 후속 PR로 재출하 |
| 2026-08-22 | `dashboard-ui` 스킬에 "인터랙션·모션 (전역 공통)" 섹션 추가 — 버튼 로딩/비활성·진행 표시·클릭 press·모션 토큰을 재사용 규약으로 명문화(새로 만들지 말 것) + 작업가이드1 상호참조 | `.claude/skills/dashboard-ui/SKILL.md`, `CLAUDE.md` | PR #104(라이브러리 0으로 인터랙션 공통화)에서 만든 프리미티브(UI.Button loading/disabled·press, UI.Progress, --dur*/duration-tok·ease-ds, indeterminate 저모션 폴백)가 다음 세션에 재발견되지 않아 화면마다 재구현되는 drift 방지. 차트 정책과 동일 계보(공통 자산 재사용 강제) |
| 2026-09-08 | `apfs-manage-page`(목업→관리형 리스트 페이지 조립 SOP)·`apfs-stage-workflow`(행 선택→단계별 컨텍스트 액션→전이) 스킬 2종 신설 + 트리거 등록, `apfs-form-modal`에 "확장: 섹션형·반복행 모달" 절 추가, `RowFormModal`에 `title` prop 추가 (`color-tokens` "상태색 칠/텍스트 토큰 분리" 절은 같은 날 PR #112에서 별도 추가) | `.claude/skills/apfs-manage-page/SKILL.md`, `.claude/skills/apfs-stage-workflow/SKILL.md`, `.claude/skills/apfs-form-modal/SKILL.md`, `.claude/hooks/skill-rules.json`, `src/dash/generic_list_modal.tsx` | 자펀드관리 목업(구조도 v1.4)을 `subfund_manage.tsx`(+`_schemas.ts`·`subfund_form_modal.tsx`)로 이식하며 정립. 영역별 스킬(grid/aggrid/form-modal/detail-filter/datepicker)은 이미 있어 **중복 스킬 대신 조립표+새 패턴 2개만** 영속화. 함정 실측: `.ag-row` 클릭은 선택 안 됨(셀 클릭), 컬럼 가상화로 뷰포트 밖 그룹헤더 DOM 부재, 숫자 N/A=null, pinned 합계 useMemo, PageSchemaZ columns 필수. 구 `subfund.tsx`(FR-5.3 대시보드)는 미라우팅 유지(삭제는 PR 결정) |
| 2026-09-09 | 페이지 생성 스킬 **세분화** — 신규 `apfs-card-view`(리스트↔카드뷰 토글·선택 유지)·`apfs-spec-popup`(읽기전용 명세 팝업) 2종 + 트리거, 기존 스킬은 보전하고 절만 추가: `apfs-grid` "프레임 외관 규약"(--frame-bg·sub 미사용·푸터 골드), `apfs-aggrid` "관리형 페이지 그리드 규약"(AUTO_SIZE_CONTENT·wrapperBorder·합계행·선택색·배지 lg), `apfs-detail-filter` "typed 페이지 트랙"(개별 칩 값만·PeriodPicker·no-op), `apfs-form-modal` 계약7·8(fit-content·inline 안티패턴), `apfs-stage-workflow` 규약 9·10(selId SSOT·명세), `dashboard-ui`(dialog forwards 블러·StatusBadge lg/dot), `apfs-manage-page` 조립표 갱신 | `.claude/skills/apfs-card-view/`, `.claude/skills/apfs-spec-popup/`, 위 7개 SKILL.md, `.claude/hooks/skill-rules.json` | PR #113·#114에서 사용자가 확정한 UI 결정 ~15건이 스킬에 없어 다음 세션이 재발명/역행할 위험. 새 스킬은 소유자가 없는 패턴 2개에만 한정(소유자 있는 규약은 해당 스킬에 dated 절로) |
| 2026-09-08 | `PeriodPicker`(`src/dash/ui/period-picker.tsx`) 표준 신설 — 연도·월·분기·반기 버튼 그리드 + 일별은 기존 `DatePicker` 위임, 값 계약 `YYYY`/`YYYY-MM`/`YYYY-Qn`/`YYYY-Hn`/`YYYY-MM-DD`. `apfs-datepicker` 스킬에 "PeriodPicker" 절 + 트리거 키워드 | `src/dash/ui/period-picker.tsx`, `.claude/skills/apfs-datepicker/SKILL.md`, `.claude/hooks/skill-rules.json` | 자펀드관리 상세필터를 원본 목업 11항목·순서로 맞추며 사업연도(연도)·기준일자(일)에 네이티브 input/연도 select 대신 표준 피커가 필요 — 사용자 지시("연도별, 월별, 일별, 분기별, 반기별로 만들어서 표준으로 저장") 영속화 |
| 2026-09-08 | `apfs-form-modal` 스킬에 "읽기전용 명세(kv) 그리드 — 라벨 배열 규약" 절 추가(한글 라벨=가로 라벨좌/값우 현행 유지, 영문 라벨=세로 적층, 테두리 유지) + 트리거 키워드(명세 팝업·kv 그리드·라벨 배열·영문 라벨) | `.claude/skills/apfs-form-modal/SKILL.md`, `.claude/hooks/skill-rules.json` | 자펀드 명세 팝업(`subfund_spec_modal.tsx`) 검토에서 세로 적층·테두리 제거안을 시도 후 사용자가 가로로 원복, 영문 버전만 세로 샘플을 채택 — 다음 세션이 같은 시안을 반복하지 않도록 결정과 근거(150px 라벨 칸 영문 줄바꿈)를 영속화 |
| 2026-09-11 | `apfs-form-modal` 스킬 3곳 갱신 — 크롬 규약5(본문 `p-[46px]` 패밀리는 헤더·푸터 `px-[46px]` 인셋, 공용 `ui/dialog.tsx` 18px 불변)+골든 예제 `DialogHeader` px 반영, 핵심계약7에 `SchemaField fill` opt-in(셀 채움=width100%+minWidth0, select/date 래퍼 분기), 확장 섹션에 반복행 테이블 규약(tableLayout:fixed·컬럼폭·thLast/tdLast 우8/0·헤더밑줄 제거·행컨트롤 fill·삭제40·섹션 mb-7·첨부 1줄 34px). `apfs-spec-popup` 골격에 헤더/푸터 46px 인셋 포인터 | `.claude/skills/apfs-form-modal/SKILL.md`, `.claude/skills/apfs-spec-popup/SKILL.md`, 메모리 1건 | PR #132(폼/명세 모달 46px 정렬 + 반복행 컨트롤 정합)에서 사용자가 확정한 UI 결정을 스킬에 영속화 — 다음 세션이 반복행 테이블에서 fit-content 겹침·auto 레이아웃 ellipsis 실패·헤더 미정렬을 재발명하지 않도록. `fill`+minWidth0은 Codex P2(240min이 200px 고정컬럼 넘침) 반영 |
| 2026-09-11 | `apfs-hotkeys` 스킬 신설 + 트리거 등록 — 앱-스코프 키보드 단축키 규약(`use-hotkey.ts`의 `HOTKEYS`=힌트+바인딩 단일 소스, `useHotkey` 훅, mod/⌥ 2티어, 레지스트리 충돌검사표, Mac/Windows 분기·⌥ 티어 Windows 미실측 함정, 3단 추가절차, `page.keyboard` trusted 검증). 겸하여 내보내기(Excel) 단축키 신설 `HOTKEYS.export`(⌘E→충돌 보고로 **⌥D**로 변경) + `subfund_manage.tsx` kebab에 배선(독립 엑셀 버튼을 kebab으로 흡수) | `.claude/skills/apfs-hotkeys/SKILL.md`, `.claude/hooks/skill-rules.json`, `src/dash/use-hotkey.ts`, `src/dash/subfund_manage.tsx` | 단축키 시스템이 `use-hotkey.ts` 인라인 주석에만 존재해 다음 세션이 규약(2티어·`e.code` 매칭·TDZ 래핑·enabled 이중열림 가드)과 레지스트리를 재발견 못하고 충돌 키를 재선택할 위험. 사용자 지시("단축키 정리해서 스킬로 저장") 영속화. Windows 실측은 미완(스킬에 판별 테스트 명시) |
| 2026-09-11 | **KPI 배지 행 규약 뒤집기(기본 포함 → 옵션·HITL)** — `apfs-grid` "KPI 배지 행" 절을 "옵션(생성 스킬 HITL 결정, 기본 미포함)"으로 개정, `apfs-capture-schema` SOP에 3.5단계(AskUserQuestion 포함여부·미포함=`hideKpis:true`), `apfs-manage-page` 조립표+SOP5에 HITL 지시. 코드: `schemas/types.ts`에 `hideKpis?:boolean`(인터페이스+zod), `generic_list.tsx` `kpis=` 계산식 최외곽에 `schema.hideKpis` 가드 추가 | `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-capture-schema/SKILL.md`, `.claude/skills/apfs-manage-page/SKILL.md`, `src/dash/schemas/types.ts`, `src/dash/generic_list.tsx` | 사용자 지시("이 부분은 선택시에만 생성 + 생성스킬에 HITL 질문 단계") — KPI 행이 자동 포함돼 모든 목업 이식 페이지에 붙던 것을 opt-in으로 전환. `hideMetrics`(금액 개념 자체 제거·카드뷰 금액까지)와 분리한 표현 전용 `hideKpis`(헤더 슬롯만) 신설 — 금액 컬럼 엔티티에서 제네릭 금액 KPI 폴백을 끄되 카드뷰는 보존. 런타임 기본값 불변(기존 스키마 `hideKpis` 미설정=무변경)이라 회귀 없음, 정본(`자펀드_공고_정보관리.ts`·`subfund_manage.tsx`)은 "포함" 반영본으로 보존 |
| 2026-09-11 | 페이지 생성 스킬에 이번 세션 레이아웃 패턴 반영 — `apfs-grid`에 "관리형 리스트 툴바·타이틀 규약" 절 신설(툴바 보조액션=kebab MoreMenu 한 곳·독립 엑셀버튼 금지·내보내기 3진입점 kebab/푸터download/⌥D, kebab MoreMenu는 공유 아님=로컬복사 골드 subfund_manage, 타이틀=메뉴 리프 라벨 일치·"○○ 목록" 금지) + `headerActions` 주석 정정(매트릭스=primary/리스트=kebab) + 골든 예제 stale `sub=` 제거(90행 규약·asset_funding 실상과 정합). `apfs-manage-page` 조립표 2행 보강(타이틀 규약·엑셀 진입점). [[apfs-hotkeys]] 크로스링크 | `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-manage-page/SKILL.md` | PR #133(엑셀 kebab 이동·타이틀 통일)에서 사용자가 확정한 리스트형 레이아웃 결정을 페이지 생성 스킬에 영속화 — 다음 세션이 툴바 독립 엑셀버튼·"○○ 목록" 임의 타이틀을 재발명하거나 MoreMenu를 공유 export로 오인하지 않도록. 매트릭스 골든은 불변(페이지 타입으로 규칙 분리) |
| 2026-09-11 | **LNB 좌측 메뉴: 현재 페이지 자동 오픈/표시**(슬라이드 하이라이트는 도입 후 같은 날 원복) — `shell.tsx`: `ancestorsOf(route)`·`LnbTree` 신설, `MenuChildren`에 `route` 전달+활성 리프 `aria-current`+primary+scrollIntoView(LnbTree 컨테이너 ref effect), `expanded` 초기화+`useLayoutEffect([route])`로 조상 그룹 자동 펼침(수동 접힘은 병합만). 배경은 **즉시 hover(`--muted`)**(활성이면 mouseleave에 primary bg 복원). menu-highlight.tsx는 원상복구(LNB용 `useMenuHighlightGroup`·null setActive 제거) | `src/dash/shell.tsx`, `src/dash/ui/menu-highlight.tsx`, `.claude/skills/dashboard-ui/SKILL.md` | 사용자 1차 지시("dropdown처럼 배경 이동 + 현재 페이지 열리고 표시")로 슬라이드 프리미티브를 LNB에 적용했으나, 2차 지시("빠르게 오버시 배경 여러 개 움직임 → 애니메이션 삭제, 그전처럼")로 **원복**. 근본 원인: 슬라이드 프리미티브는 열렸다 닫히는 **팝업 전용**(닫힘=unmount 리셋)이라, **상주 네비게이션**에선 활성 페이지 지속표시+잔류 슬라이드가 겹쳐 다중 배경. dashboard-ui·menu-highlight 헤더에 "상주 네비 금지" 규약 명문화. 자동오픈+활성표시(1차 요청)는 즉시 hover로 유지. aside repl 검증: slideSpans=0·빠른 hover 후 배경 버튼=1(활성만)·자동오픈(curH25)·활성 1개 통과 |
| 2026-09-11 | **등록 버튼을 kebab 밖 툴바로 승격(규약 뒤집기)** — `subfund_manage.tsx`: kebab `MoreMenu`의 `제안서접수 등록` 항목·separator·`onRegister` prop 제거, `toolbarRight`에 `Button variant="outline" leadingIcon="plus"` 신설(상세필터→등록→새로고침→kebab), 푸터 폴백 kebab도 동기화. 스킬 3종 동기화: `apfs-grid` "관리형 리스트 툴바·타이틀 규약" 전면 개정(순서 다이어그램·outline 위계·generic_list 미반영 트랙 표기), `apfs-manage-page` 조립표에 "신규 등록(1차 액션)" 행 추가, `apfs-hotkeys`에 ⌘⏎ 힌트 소실 각주 | `src/dash/subfund_manage.tsx`, `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-manage-page/SKILL.md`, `.claude/skills/apfs-hotkeys/SKILL.md` | 사용자 결정 — 등록은 이 화면의 1차 액션인데 kebab 안에 있어 2클릭이 걸렸다. 검증: `npm run build` exit 0 · `npm test` 51/51 · 브라우저 라이트/다크 툴바+kebab 항목 실측 · 380px에서 툴바 2행 wrap(오버플로 없음) |
| 2026-09-11 | **스키마 주도 트랙(generic_list)에 관리형 리스트 툴바 규약 이식** — `generic_list.tsx`: kebab `MoreMenu`에서 `등록`·separator·`onRegister`/`editable` prop 제거하고 골드(`subfund_manage`) 동형으로 교체(Tooltip 래핑·`DropdownMenuShortcut` ⌥D/⌘P 힌트·`size` prop·인쇄 `window.print()` 배선), `toolbarRight`에 등록 버튼 신설(`{editable && <Button variant="outline" leadingIcon="plus">{schema.entity + ' 등록'}</Button>}` — 상세필터→등록→새로고침→kebab), `IconBtn refresh` 라벨 `조회`→`새로고침`, 푸터의 onClick 없는 죽은 `IconBtn more`를 `!topMoreVisible && <MoreMenu size={32}>` 폴백으로 교체(+`topMoreRef` IntersectionObserver), `maximize`에 `pressed={showAll}`, 핫키 3종(⌘⏎ 등록은 `enabled: editable && modal === null`) 배선. 스킬 동기화: `apfs-grid`의 "미반영 트랙" 경고를 반영 완료로 뒤집고 푸터 골드의 stale `IconBtn more` 표기 정정 | `src/dash/generic_list.tsx`, `.claude/skills/apfs-grid/SKILL.md` | 사용자 요청(자펀드 공고 정보관리 페이지를 그리드 스킬 배치에 맞춤). 이 페이지는 bespoke가 아니라 스키마 주도 트랙이라 대상이 공유 셸 `generic_list.tsx` — 규약 통일이 목적이므로 6개 스키마 페이지 전체에 동시 적용. 검증: `npm run build` exit 0 · `npm test` 51/51 · aside repl 실측(툴바 순서·kebab 2항목+단축키 힌트·전체보기 스크롤 시 푸터 폴백 kebab 2개 출현·`fields` 없는 스키마(연도별투자현황)는 등록 버튼 미노출·라이트/다크·툴바 `flex-wrap:wrap`). Codex 리뷰 4건 중 3건 반영: ①`IntersectionObserver` 미지원 시 `setTopMoreVisible(false)`로 폴백 상시 노출(true 고정이면 푸터 kebab이 영원히 안 떠 내보내기·인쇄 접근 소실) ②⌘⏎ `enabled`에 `!filterOpen && ctx === null` 추가(상세필터 Sheet·컨텍스트 메뉴 위에 등록 모달이 겹쳐 열려 포커스 트랩을 깨뜨림 — 실측: 드로어 열림 시 dialog 1→1 차단, 닫은 뒤 1개 정상 개폐) ③`use-hotkey.ts` 효과 deps에 `combo.alt` 추가(현재 `HOTKEYS`가 모듈 상수라 benign). 미반영 1건: ⌥D의 Windows Alt+D(주소창) 충돌 — 이번 diff가 만든 게 아니라 기존 `HOTKEYS.export` 규약이고 [[apfs-hotkeys]]에 "⌥ 티어 Windows 미실측" 함정으로 이미 기록됨. 여기서만 바꾸면 골드 `subfund_manage`와 어긋나므로 단축키 스킬 차원에서 일괄 판단할 사안. ⚠️ 잔존: 골드 `subfund_manage.tsx:281`에도 동일한 `IntersectionObserver` 미지원 결함이 남아 있다(요청 범위 밖이라 미수정) |
| 2026-09-11 | **자펀드 공고 정보관리: KPI 배지 행·카드뷰 토글 제거 + `hideCardView` 플래그 신설** — `schemas/types.ts`에 표현 전용 `hideCardView?: boolean`(인터페이스+zod) 추가, `generic_list.tsx`는 `const [viewState, setView]` + `const view = schema.hideCardView ? "list" : viewState` 파생 고정 & 푸터 `SegTabs`를 `{!schema.hideCardView && …}`로 게이트, `schemas/자펀드_공고_정보관리.ts`는 `countKpis` 3배지(전체 건수·농식품·수산)를 제거하고 `hideKpis: true` + `hideCardView: true` 선언. `apfs-grid` 스킬의 KPI 정본 예시를 "미포함 예시"로 정정하고 카드뷰 opt-out 규약 추가 | `src/dash/schemas/types.ts`, `src/dash/schemas/자펀드_공고_정보관리.ts`, `src/dash/generic_list.tsx`, `.claude/skills/apfs-grid/SKILL.md` | 사용자 지시(스크린샷 첨부, "전체건수 카드 3개 모두 삭제하고 카드보기·리스트보기 버튼도 삭제"). 적용 범위는 HITL로 확인해 **이 페이지만**(다른 5개 스키마 페이지는 현행 유지) — 카드뷰 토글이 공유 셸 하드코딩이라 스키마 플래그로 opt-out 경로를 새로 냈다. 표현 전용 플래그 3종(`hideKpis`/`hideMetrics`/`hideCardView`)은 서로 독립. 검증: `npm run build` exit 0 · `npm test` 51/51 · aside repl 실측(공고 페이지 KPI·카드뷰·리스트뷰 문자열 전부 부재, 등록 버튼 잔존 / 회귀: 연도별투자현황·투자기업정보(통합)·조합별 월간보고 현황 3개 모두 KPI·카드뷰 유지) |
| 2026-09-11 | **자펀드 관리(typed)도 KPI 배지 행·카드뷰 토글 제거** — `subfund_manage.tsx`: `kpis` prop(전체 건수·결성 조합·약정총액 합계 3배지)과 KPI 전용 파생값 `totalCommit`·`formedCount` 삭제, `footerRight`의 `SegTabs` 삭제, `const [view, setView] = useState('list')` → `const view = 'list'` 상수화(setView 호출처가 SegTabs뿐이었음), 미사용이 된 `KpiBadge`·`SegTabs` import/구조분해 정리. 카드 렌더 분기(`view === 'detail'`)는 복구 대비로 잔존. `apfs-grid` 스킬의 KPI 정본 예시와 `hideCardView` 절에 typed 트랙 지침 추가 | `src/dash/subfund_manage.tsx`, `.claude/skills/apfs-grid/SKILL.md` | 사용자 지시("자펀드관리 메뉴도 동일하게 삭제") — 직전 자펀드 공고 정보관리(스키마 트랙, `hideKpis`/`hideCardView` 플래그)와 같은 결과를 typed 트랙에서는 플래그 없이 직접 제거로 달성. 검증: `npm run build` exit 0 · `npm test` 51/51 · aside repl 실측(`전체 건수`/`결성 조합`/`약정총액 합계`·`카드뷰`·`리스트 뷰` 문자열 전부 부재, `제안서접수 등록` 버튼·심사단계 필터칩·pinned 합계행·단위 캡션 잔존) |
| 2026-09-11 | **카드뷰 규약 전면 폐기 — `apfs-card-view` 스킬 삭제** — 스킬 파일(`SKILL.md`) 제거 + `skill-rules.json`의 트리거 블록 제거(키워드 `카드뷰`·`리스트 뷰`·`뷰 토글` 등), 상호참조 4곳 정리: `apfs-grid`(푸터 골드에서 `SegTabs 리스트 뷰|카드뷰` 제거·`footerCenter` 게이트에서 `view==='list'` 제거·카드뷰 절을 ⛔폐기 규약으로 교체·KPI 절의 "카드뷰 금액/상태" 서술 정정), `apfs-aggrid`·`dashboard-ui`·`apfs-manage-page`(스킬 색인 링크 제거, 조립표의 "리스트 ↔ 카드뷰" 행을 폐기로 변경) | `.claude/skills/apfs-card-view/SKILL.md`(삭제), `.claude/hooks/skill-rules.json`, `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-aggrid/SKILL.md`, `.claude/skills/apfs-manage-page/SKILL.md`, `.claude/skills/dashboard-ui/SKILL.md` | 사용자 지시("그리드 스킬에서 카드뷰/리스트뷰 삭제", 범위는 HITL로 "카드뷰 규약 전체 폐기" 선택). 앞서 자펀드 공고 정보관리·자펀드 관리 두 화면에서 뷰 토글을 제거한 결정의 규약화 — 신규 페이지가 카드뷰를 재발명하지 않도록 스킬 계층에서 경로를 끊었다. **코드는 이번 범위 밖** — `generic_list.tsx`의 카드 렌더 분기와 나머지 스키마 페이지 4개의 토글은 그대로 남아 있다(`hideCardView` 플래그로 끌 수 있는 상태). `skill-rules.json`은 `json.dumps` 재직렬화가 전체 포맷을 갈아엎어(469줄 diff) 되돌리고 중괄호 균형 기반 텍스트 절삭으로 7줄만 제거 |
| 2026-09-12 | **하네스 감사 + drift 3건 해소** — ①`CLAUDE.md`의 프로젝트 스킬 열거(4개)를 **포인터로 교체**(`.claude/skills/` + `skill-rules.json`이 정본, `ls`로 확인) — 실제 17개라 13개 누락 상태였고 열거 자체가 재발원이라 harness 스킬 Phase 5-4("CLAUDE.md에 스킬 목록을 넣지 않는다")를 따랐다. ②`apfs-form-modal` 트리거에 복합어 `등록 모달` 추가(`수정 모달`은 있는데 `등록 모달`만 없던 비대칭 누락). 함께 시도한 `모달 필드`는 **무효로 판명돼 제거** — 한국어 조사 삽입("모달**에** 필드")으로 인접 리터럴 매칭이 깨지고, 리터럴이 그대로 나오는 문장은 기존 `필드 컨트롤`이 이미 잡는다. ⚠️ 알려진 미탐 잔존: `모달에 필드 추가` 계열 — 단독 `모달`을 넣으면 해소되나 `responsive-ui`·`z-index`가 이미 보유해 3중 발화가 되므로 **의도적으로 남긴다**. ③에이전트 5종 `model:`을 full ID(`claude-opus-4-8`·`claude-sonnet-4-6`)에서 **별칭**(`opus`·`sonnet`)으로 통일 + `CLAUDE.md` Agent 표 동기화 — **2026-06-20 '모델 핀' 결정의 의도적 역전**: 글로벌 표준(`~/.claude/agents/architect.md`·`worker.md` = `model: opus`, "자동 폴백은 두지 않는다")과 어긋났고, 세대번호가 박힌 ID는 모델 갱신 때마다 낡으며 해석 실패 시 무음 폴백과 성공이 구분되지 않는다 | `CLAUDE.md`, `.claude/hooks/skill-rules.json`, `.claude/agents/*.md`(5) | 자동연동 훅 점검 요청에서 출발한 전면 감사. **훅 자체는 end-to-end 실측 통과**(`z-index 확인해줘` → 컨텍스트에 `[SKILLS ACTIVATED] HIGH: z-index` 주입 확인) — 파일 존재·JSON 유효성·스크립트 단독 실행이 다 통과해도 등록 경로가 끊기면 무음이라, 키워드 1개짜리 프롬프트가 유일한 end-to-end 테스트였다. rules↔skills drift 0건·frontmatter 17/17 일치·동시발화 최대 7(복합 프롬프트)/일반 2~3으로 정상. 검증: 미탐 RED→GREEN(`등록 모달 만들어` → form-modal 발화), 무관 프롬프트 4종 과발화 0건, `jq empty` ok, diff 1줄(재직렬화 회피) ⚠️ **미해결(사용자 판단 대기)**: `intentPatterns`가 17/17 rule에 정의돼 있으나 jq 경로·grep 폴백 **둘 다** `.keywords[]`만 검사한다 — 즉 이 파일에서 작성된 모든 트리거 규칙이 절반은 무음이었고, 과거 이력의 "트리거 등록" 중 intentPattern에 기댄 부분은 발효되지 않았다. `skill-activator.sh:9` 주석("키워드/intentPatterns 와 매칭")은 사실과 다르고 `_comment`("키워드 매칭으로 사용")만 정확 — 스크립트가 자기 문서와 모순. 선택지: (a) 스크립트에 패턴 매칭 추가 + 팬아웃 회귀 측정(패턴이 키워드보다 훨씬 넓어 현재 기준선 일반 2~3·최대 7이 움직인다) vs (b) 주석 9행을 사실로 정정하고 필드는 문서값으로 보존. 키워드 커버리지가 이미 양호(15개 중 14 발화)하므로 **(b) 권장** |
| 2026-09-12 | **`intentPatterns` 무음 문제 — 문서 정정으로 종결(옵션 b)** — `skill-activator.sh` 주석 2행("키워드/intentPatterns 와 매칭한다")을 사실로 정정: `keywords` 전용이며 `intentPatterns` 는 jq 경로·grep 폴백 **모두** 읽지 않고, 규칙 파일에 남은 값은 '이 스킬이 어떤 의도에서 떠야 하는가'를 적은 문서값이라는 점 + "트리거 추가는 반드시 keywords 에"를 명시. `skill-rules.json` 의 `_comment` 에도 같은 경고 추가(규칙 편집자가 오해하는 지점 차단). **동작 변경 없음** | `.claude/hooks/skill-activator.sh`, `.claude/hooks/skill-rules.json` | 2026-09-12 감사에서 발견한 '스크립트가 자기 문서와 모순' 상태 해소. (a) 패턴 매칭을 실제로 구현하는 대신 (b) 문서 정정을 택한 근거: 패턴(`(등록\|수정\|crud\|입력\|폼\|form).*(모달\|modal\|양식)` 류)은 키워드보다 훨씬 넓어 측정된 팬아웃 기준선(일반 2~3·최대 7)을 흔드는데, 키워드 커버리지가 이미 양호(실사용 15개 중 14개 발화)해 얻을 이득이 회귀 리스크를 넘지 않는다. 필요해지면 (a)는 이 행을 근거로 다시 열 수 있다. 검증: 주석 변경이므로 발화 불변을 실측(`등록 모달`·`z-index`·`AG Grid 합계행`·`스크린리더 라벨` 4종 동일), `bash -n` ok, `jq empty` ok. ※grep 폴백 경로는 `/usr/bin/jq` 가 잡혀 시뮬 실패 — 미검증이나 이번 diff가 건드리지 않은 경로 |
| 2026-09-12 | **⚠검토필요 마커 규약 뒤집기(이식 안 함 → 이식함)** — 공용 `src/dash/review_marker.tsx` 신설(`ReviewMarker`·`reviewInnerHeader`, Radix Popover 제어형), `occasional_report_manage.tsx` 심사담당·리스크담당 헤더에 배선. 스킬 3종 동기화: `apfs-grid`에 "검토필요 마커" 정본 절 신설(2줄 고정 추천/데이터·라벨에만·마스크/엑셀 비대상·`--warning-text`·다크 칩 배경 함정), `apfs-aggrid`에 "헤더에 ⚠검토필요 마커 달기"(innerHeaderComponent·정렬 버블링·Tab 가로채기·폭 상향·React 헤더 늦은 마운트), `apfs-manage-page` 41행의 "이식하지 않는다" 문장 예외 처리 | `src/dash/review_marker.tsx`, `src/dash/occasional_report_manage.tsx`, `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-aggrid/SKILL.md`, `.claude/skills/apfs-manage-page/SKILL.md` | 사용자 지시("이부분 누락됐어 추가하고 그리드 스킬에도 추가해") — 원문 미정의 지점을 화면에서 바로 보여주는 설계 메모라 GNB/LNB 토글 같은 프로토타입 스캐폴딩과 성격이 다르다. 실측 함정 3건: 마커 클릭이 헤더로 버블링돼 정렬 발화(stopPropagation), AG Grid가 Tab을 가로채 마커에 키보드 도달 불가(`suppressHeaderKeyboardEvent`), Enter는 팝오버를 열면서 정렬까지 발화·Space는 AG Grid 캡처 preventDefault로 click이 안 생김(Codex 1R P2 → 제어형 Popover로 직접 토글). 검증: build exit 0 · test 51/51 · aside repl 라이트/다크 + 키보드(Enter·Space·Escape·방향키·헤더 Enter 정렬) · Codex 2R clean |
| 2026-09-12 | ⚠검토필요 마커를 상세필터 드로어로 확대 — `subfund_manage.tsx`(심사담당자·리스크담당자)·`investment_review_manage.tsx`(담당자) 라벨에 배선, 각 페이지 로컬 `DrawerField`에 `note?: ReviewNote` prop 추가, 두 파일 헤더의 "마커 이식 안 함" 주석 정정. 스킬: `apfs-grid` "붙는 위치" 불릿에 드로어 패턴·label 안 button 규칙·z 순서 추가, `apfs-detail-filter` 참조 링크 | `src/dash/subfund_manage.tsx`, `src/dash/investment_review_manage.tsx`, `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-detail-filter/SKILL.md` | 사용자 선택(①마커 달기) — 규약 반전 후 두 파일만 옛 주장을 담고 있어 다음 세션이 모순을 만난다. 부수: 트리거를 `<button>`→`<span role=button>`으로 교체(button 은 labelable 이라 `<label>` 안에서 select 의 암묵 연결을 가로챔), click·mousedown preventDefault + 직접 토글(크롬 라벨 포커스 전달로 팝오버가 즉시 닫히던 문제), 마스크 ON 시 헤더 스켈레톤 바 위로 올리는 `position/z-index`, 팝오버 `role=dialog` 접근名 — 모두 Codex 3라운드 지적 반영·실측. 문구는 `자펀드관리_목업.html` 258·259행 `data-rec`/`data-dat` 원문. ⚠ `investment_review`의 출처 목업 S1_01은 저장소에 없어 동일 담당자 옵션(양한솔·이성훈)을 근거로 문구를 차용했고 코드 주석에 명시 — 원문 확인 시 교체 필요 |
| 2026-09-12 | 검토필요 마커 외관·상호작용 개편 — `!` 글리프/soft 배경 → **무채색 lucide `Info` 원**(기본 muted+opacity .8, hover·focus·open 시 `--warning-text` 강조, 포인터 타깃 20px), **hover로 열기**(140ms 유예·팝오버 위 유지) 추가, 클릭은 열기 전용으로 변경(hover와 토글 충돌), 포커스 이동·복귀는 키보드 개방에만 | `src/dash/review_marker.tsx`, `.claude/skills/apfs-grid/SKILL.md` | 사용자 피드백 2단계: ①"색상이 눈에 안 띈다·마우스오버로 보이게·lucide 아이콘" → 솔리드 amber 칩 + `TriangleAlert` 시도, ②"너무 촌스럽다" → 시안 8종 렌더링 후 사용자가 **무채색 info 원** 선택. 중간 단계에서 추가했던 `--on-warning-solid` 토큰은 소비처가 사라져 회수(tokens.css·color-tokens 스킬 원복). 검증: build exit 0 · test 51/51 · 기본/hover 색 실측(라이트 #5E6B60→#8A5A00, 다크 #9AA694→#FBC04A) · hover 열림·팝오버 이동 유지·이탈 닫힘·클릭 개방·키보드 Enter/Space/Escape·정렬 미발화 · hover 닫힘이 입력 중 포커스를 뺏지 않음(Codex 지적 반영, onCloseAutoFocus 분기) |
| 2026-09-12 | 검토필요 마커 **원문 전수 이식** — 사용자가 목업 원본 경로(`~/Downloads/통합/…`)를 제공해 `S1_04_수시보고.html` 5건(검색 심사담당자·리스크담당자·구분 + 확인 컬럼 2건)과 `S1_01_투자심의관리.html` 3건(운용사·자펀드·담당자)을 원문 그대로 배선. 확인 컬럼 메모를 역할별로 분리(원문이 '심사담당'/'리스크담당'으로 갈림 — 이전엔 양쪽에 심사담당 문구를 복제해 오기), 투자심의 화면의 **차용 메모를 원문으로 교체**. `apfs-grid` 스킬에 원문 경로·추출 명령·전수 이식 규칙 추가 | `src/dash/occasional_report_manage.tsx`, `src/dash/investment_review_manage.tsx`, `.claude/skills/apfs-grid/SKILL.md` | 필터 영역 마커 누락 지적(사용자). 교훈: 마커는 검색영역·그리드헤더 양쪽에 흩어져 있어 한쪽만 보면 빠진다. 검증: build exit 0 · test 51/51 · 8개 마커 렌더 확인 · 메모 본문 3건 원문 대조(리스크담당·구분·자펀드) |
| 2026-09-12 | 검토필요 마커 스킬 정본화 — `apfs-grid` "검토필요 마커" 절을 누적 레이어에서 **현재 상태 기준으로 재작성**(내용·붙는 위치/마스크 경계·외관·상호작용·a11y 5소절, 실증 3화면과 건수 명시), `apfs-aggrid` 예시를 역할별 `CONFIRM_HEADER` 맵으로 교체, `apfs-detail-filter` 포인터를 `note` prop 실사용형으로 구체화, `review_marker.tsx` 파일 헤더 주석을 최종 사양으로 갱신(정본은 스킬이라고 명시) | `.claude/skills/apfs-grid/SKILL.md`, `.claude/skills/apfs-aggrid/SKILL.md`, `.claude/skills/apfs-detail-filter/SKILL.md`, `src/dash/review_marker.tsx` | 사용자 지시("스킬에 변경내용 적용해"). 세 차례 설계 변경(외관 3안·hover 도입·원문 전수 이식)이 절 안에 층층이 쌓여 폐기된 사양(soft 배경 `!` 글리프·솔리드 amber)이 현행처럼 읽히던 drift 해소 — 폐기안은 "되돌리지 말 것" 한 줄로만 남김 |
